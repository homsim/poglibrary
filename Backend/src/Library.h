#include<vector>

class Library
{
    /*
    Holds informations about the library. Should be created upon startup
    either from scratch (empty library) or from the database.    
    */
private:
    /* data */
public:
    Library(/* args */);
};
